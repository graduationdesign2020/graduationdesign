var sub = function(val) {
  if (val == undefined)
    return
  if (val.length == 0) {
    return;
  }
  if (val.length > 20) {
    return val.substring(0, 20) + "...";
  } else {
    return val;
  }
}
module.exports.sub = sub;