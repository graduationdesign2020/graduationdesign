server:
  port: 8401

spring:
  application:
    name: GDMS-Monitor-Admin
  boot:
    admin:
      ui:
        title: ${spring.application.name}
  security:
    user:
      name: gdms
      password: gdmsadmin
